# Claude-Specific Context

See [`agents.md`](./agents.md) for the main project context.

## Tips

- Read existing files before creating new ones — match the style
- Events live in `src/content/event/[topic]/`, organizers in `src/content/organizer/`
- Check `src/content.config.ts` for the schema (Zod validation)
- Use `bun run build` to verify changes compile
- Use Edit tool for existing files, Write for new ones
