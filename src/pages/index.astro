---
import Layout from '@layouts/Layout.astro'
import { getEvents, getLanguages } from '@lib/event'
import Events from '@components/Events.astro'

const languages = await getLanguages()
---

<Layout>
  <Events />
</Layout>
